# BL NI Concept

If the client consumes <=3 BLs in the MCAT and submits negative feedback for >=3 BL in the same MCAT then the MCAT will be marked as negative automatically from the backend.Make sure that the client has selected the option ' I don't deal in <MCAT Name>' for sharing the feedback.
